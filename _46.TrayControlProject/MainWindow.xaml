<Window x:Class="_46.TrayControlProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_46.TrayControlProject" xmlns:controls="clr-namespace:_46.TrayControlProject.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <controls:TrayControl Count="60" x:Name="myTrayControl"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Margin="5">
                <Run Text="数量："/>
                <Run Text="{Binding ElementName=myTrayControl, Path=Count}"/>
            </TextBlock>
            <Rectangle Width="10"/>
            <TextBlock Margin="5">
                <Run Text="选中数量： "/>
                <Run Text="{Binding ElementName=myTrayControl,Path=SelectedCount}"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
