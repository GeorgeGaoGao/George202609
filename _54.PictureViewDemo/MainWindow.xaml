<Window x:Class="_54.PictureViewDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_54.PictureViewDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="900" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Canvas Name="myCanvas" Background="LightBlue"
            MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
            MouseLeftButtonUp="Canvas_MouseLeftButtonUp"
            MouseMove="Canvas_MouseMove"
            MouseWheel="Canvas_MouseWheel">
            <Image Name="myImage" Source="/Images/001.jpg" />
        </Canvas>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10" Foreground="Red" FontSize="16">
            <Run Text="X: "/>
            <Run Text="{Binding X}"/>
            <Run Text="Y: "/>
            <Run Text="{Binding Y}"/>
            <Run Text="Delta: "/>
            <Run Text="{Binding Delta}"/>
        </TextBlock>
        <Grid Name="myGrid" MouseMove="myGrid_MouseMove">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0 0" EndPoint="1 0">
                    <LinearGradientBrush.GradientStops>
                        <GradientStop Color="green" Offset="0"/>
                        <GradientStop Color="#008080" Offset="0.5"/>
                        <GradientStop Color="Blue" Offset="1"/>
                    </LinearGradientBrush.GradientStops>
                </LinearGradientBrush>                
           
            </Grid.Background>
            <Border BorderBrush="Red" BorderThickness="2"  Width="500" Height="500" Background="White">
                <Ellipse MouseMove="myEllipse_MouseMove" Width="200" Height="200" Name="myEllipse">
                    <Ellipse.Fill>
                        <RadialGradientBrush GradientOrigin="0.5 0.5" RadiusX="0.5" RadiusY="0.5" >
                            <RadialGradientBrush.GradientStops>
                                <GradientStop Color="White" Offset="0"/>
                              
                                <GradientStop Color="red" Offset="0.65"/>
                                <GradientStop Color="Gray" Offset="0.85"/>
                            </RadialGradientBrush.GradientStops>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
            </Border>
        </Grid>
    </Grid>
</Window>
