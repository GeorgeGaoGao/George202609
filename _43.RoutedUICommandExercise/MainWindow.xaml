<Window x:Class="_43.RoutedUICommandExercise.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_43.RoutedUICommandExercise"
        mc:Ignorable="d" FontSize="20" Name="myWindow"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <RoutedUICommand x:Key="PlayCommand" Text="Play"/>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="{StaticResource PlayCommand}"
                        Executed="CommandBinding_Executed"
                        CanExecute="CommandBinding_CanExecute"/>
    </Window.CommandBindings>
    <Window.InputBindings>
        <MouseBinding Gesture="Control+WheelClick" Command="{StaticResource PlayCommand}"/>
        <KeyBinding Gesture="Control+s" Command="{StaticResource PlayCommand}"/>
    </Window.InputBindings>
    <StackPanel>
        <Button Width="100" Content="Play"  Margin="10" Height="50" Command="{StaticResource PlayCommand}"
                CommandTarget="{Binding ElementName=myWindow}"/>
    </StackPanel>
</Window>
