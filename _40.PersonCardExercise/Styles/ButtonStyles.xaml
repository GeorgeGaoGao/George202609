<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="Button" x:Key="PersonCardButtonStyle">
        <Style.Setters>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="Height" Value="150"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Content" Value="PersonCard"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" Background="{TemplateBinding Background}"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                BorderBrush="Gray" CornerRadius="5"
                                BorderThickness="1" >
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="25" Margin="40 10 10 0"/>
                                    <TextBlock Text="{TemplateBinding Tag}" FontSize="16" Margin="40 0 0 0"/>
                                </StackPanel>
                                <StackPanel Grid.Row="1">
                                    <TextBlock Text="{Binding Age}" HorizontalAlignment="Right" Margin="0 0 40 0"/>
                                    <TextBlock Text="{Binding Address}" HorizontalAlignment="Right" Margin="0 0 40 0"/>
                                </StackPanel>
                            </Grid>
                            <Border.ToolTip>
                                <ContentPresenter/>
                            </Border.ToolTip>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="LightGreen" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>
    
</ResourceDictionary>