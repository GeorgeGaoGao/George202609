<Window x:Class="_58.DoubleAnimationExercise.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_58.DoubleAnimationExercise"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Storyboard x:Key="WidthStoryBoard" >
            <DoubleAnimation Duration="0:0:1.5" To="300" AutoReverse="True" RepeatBehavior="Forever" Storyboard.TargetProperty="Width"/>
        </Storyboard>
        <Storyboard x:Key="HeightStoryBoard">
            <DoubleAnimation Duration="0:0:1.5" To="300" AutoReverse="True" RepeatBehavior="Forever" Storyboard.TargetProperty="Height"/>
        </Storyboard>
    </Window.Resources>
    <Grid>
        <Ellipse Width="200" Height="200">
            <Ellipse.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <EventTrigger.Actions>
                        <BeginStoryboard Storyboard="{StaticResource WidthStoryBoard}"/>
                        <BeginStoryboard Storyboard="{StaticResource HeightStoryBoard}"/>
                    </EventTrigger.Actions>

                </EventTrigger>
            </Ellipse.Triggers>
            <Ellipse.Fill>
                <RadialGradientBrush GradientOrigin="0.25 0.25" RadiusX="0.75" RadiusY="0.75">
                    <RadialGradientBrush.GradientStops>
                        <GradientStop Color="White" Offset="0"/>
                        <GradientStop Color="Goldenrod" Offset="0.65"/>
                        <GradientStop Color="Gray" Offset="0.8"/>
                    </RadialGradientBrush.GradientStops>
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>
    </Grid>
</Window>
