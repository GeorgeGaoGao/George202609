<Window x:Class="_36.DatatriggerExercise.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_36.DatatriggerExercise"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <local:AgeToLevelConverter x:Key="AgeToLevelConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DataGrid ItemsSource="{Binding People }" AutoGenerateColumns="False" SelectedItem="{Binding Person}"
                  CanUserAddRows="False">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Age,Converter={StaticResource AgeToLevelConverter}}" Value="0">
                            <Setter Property="Background" Value="DarkGreen"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Age,Converter={StaticResource AgeToLevelConverter}}" Value="1">
                            <Setter Property="Foreground" Value="Red"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Age" Binding="{Binding Age}"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Column="1" Margin="10 5">
            <StackPanel Orientation="Horizontal">
                <TextBlock>
                    <Run Text="Name: "/>
                    <Run Text="{Binding Person.Name}"/>
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock>
                    <Run Text="Age: "/>
                    <Run Text="{Binding Person.Age}">
                        <Run.Style>
                            <Style TargetType="Run">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Person.Age}"  Value="52" >
                                        <Setter Property="Background" Value="Orange"/>
                                    </DataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Person.Name}" Value="George02"/>
                                            <Condition Binding="{Binding Person.Age}" Value="52"/>
                                        </MultiDataTrigger.Conditions>
                                        <MultiDataTrigger.Setters>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                         </MultiDataTrigger.Setters>
                                    </MultiDataTrigger>
                                                 
                                </Style.Triggers>
                            </Style>
                        </Run.Style>
                    </Run>
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock>
                    <Run Text="Address: "/>
                    <Run Text="{Binding Person.Address}"/>
                </TextBlock>
            </StackPanel>
            <Button Content="增加人员" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
